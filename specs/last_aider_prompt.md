Create EventMonitor service class in app/lib/services/event-monitor.ts with startMonitoring() and stopMonitoring() methods. Implement polling logic to check localStorage for events every 60 seconds. Implement time window check to filter events where startTime falls within ±5 seconds of current time. Implement duplicate prevention by tracking notified events by eventId + time window to prevent duplicate notifications. Create NotificationService class in app/lib/services/notification-service.ts with triggerNotification(event) method. Implement notification creation: create NotificationState object and dispatch ADD_NOTIFICATION action. Create NotificationPopup component in app/components/notifications/NotificationPopup.tsx using ShadCN UI Sonner/Toast. Implement notification display: show event title and scheduled time, mobile-responsive design (iPhone 15 optimized). Create audio service in app/lib/services/notification-service.ts to playNotificationSound() using HTML5 Audio API. Add notification sound file to public/sounds/notification.mp3. Integrate audio playback in app/lib/services/notification-service.ts: play sound by default when notification triggers, handle browser autoplay policies. Create NotificationManager client component in app/components/notifications/NotificationManager.tsx that initializes EventMonitor on mount. Implement tab visibility check in app/lib/services/event-monitor.ts: only trigger notifications when browser tab is active (FR-006 requirement). Add NotificationManager to app layout in app/layout.tsx to start event monitoring when app loads.